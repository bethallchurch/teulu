type Todo @model {
  id: ID!
  name: String!
  description: String
  completed: Boolean
}

type User @model {
  cognitoId: ID!
  id: ID!
  username: String!
  confirmed: Boolean!
  phoneNumber: String
}

import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const CreateAlbum = ({ navigation }) => (
  <SafeAreaView>
    <Text>Create album placeholder</Text>
  </SafeAreaView>
)

export default CreateAlbum
